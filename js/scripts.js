function numberToNumeral (number) {
  let numeral1 = "I";
  let numeral = [];
  if (number <= 3) {
    numeral.push(numeral1.repeat(i));
  } else if (number === 4) {
    numeral = "IV"
  } else is (number === 5) {
    numeral = "V"
  }
  return numeral;
}

function numeralTens (number) {
 //let numArray = [];
 let num = []
 for (let i =0; i <=3; i++){
 num.push(i += I)
}
}
function numberToNumeral (number) {
let numeral = [];
for (let i = 0; i <= )
}

const numerals = {I:1, IV:4, V:5, X:10, L:50, C:100, D:500, M:1000, CM:900, CD:400, XC:90, IX:9, XV:40};

function numberToNumeral (number) {
  let numeral1 = "I";
  let numeral = [];
  for (let i =0; number <=3; i++){
    numeral.push(numeral1.repeat(i))
    
  }
  return numberToNumeral;
}